name: Quick Unit Tests

on:
  push:
    branches:
      - main
      - develop
      - 'release/**'
    paths:
      - 'main_backend/**'
      - 'main_frontend/**'
      - '.github/workflows/quick-tests.yml'
  pull_request:
    branches:
      - main
      - develop
      - 'release/**'
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Backend Unit Tests Only
  test-backend:
    name: Backend Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Run unit tests
        run: |
          cd main_backend
          mvn clean test -Dspring.profiles.active=test
        env:
          SPRING_PROFILES_ACTIVE: test

      - name: Test Summary
        if: always()
        run: |
          cd main_backend
          echo "=== Test Results ==="
          if [ -d "target/surefire-reports" ]; then
            echo "Tests completed. Check reports for details."
            find target/surefire-reports -name "*.txt" -exec cat {} \; | grep -E "Tests run:|BUILD" || true
          fi

  # Frontend Unit Tests Only
  test-frontend:
    name: Frontend Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: main_frontend/concert1/package-lock.json

      - name: Install dependencies
        run: |
          cd main_frontend/concert1
          npm ci --legacy-peer-deps

      - name: Run unit tests
        run: |
          cd main_frontend/concert1
          npm run test

      - name: Test Summary
        if: always()
        run: |
          echo "=== Frontend Tests Complete ==="

  # Summary Job
  test-summary:
    name: Test Summary
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check test results
        run: |
          echo "📊 Test Pipeline Summary"
          echo "========================"
          echo "Backend Tests: ${{ needs.test-backend.result }}"
          echo "Frontend Tests: ${{ needs.test-frontend.result }}"
          echo ""
          if [ "${{ needs.test-backend.result }}" == "success" ] && [ "${{ needs.test-frontend.result }}" == "success" ]; then
            echo "✅ All tests passed!"
            exit 0
          else
            echo "❌ Some tests failed"
            exit 1
          fi
